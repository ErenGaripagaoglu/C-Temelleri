#include <stdio.h> // library import for "standart input-output . header"

int main() { //create main execution function
    printf("Hello, World!\n"); //print output
    return 0; //return a placeholder value because the function requires integer value for return
}
